# UI 精细化（Apple 式微雕）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在已交付的零 Material 设计系统上做产品级 UI 精细化：行内元数据双行式、smart 视图 sticky 分区白卡片、侧边栏增强、详情页丰富、空状态线稿插画、暗色同精度。

**Architecture:** 数据层仅加 `flag` 列（completedAt 已存在、时间已由 due_date 携带）。UI 层重写 TodoRow（双行式）、TodoListScreen（头部+分区卡片）、Sidebar（分组/计数/hover 操作）、DetailScreen（结合版）、RemEmptyState（线稿插画），扩展 DesignTokens（新 token）、RemIcons（Flag 等新图标）、RemDatePicker（时间步进器）、RemBadge（可点击）。

**Tech Stack:** Kotlin Multiplatform + Compose Multiplatform（commonMain）、SQLDelight、kotlinx-datetime、ViewModel。测试：kotlin.test + runTest（desktopTest 跑 SQLDelight，commonTest 跑 ViewModel）。

**规格:** `docs/superpowers/specs/2026-08-11-ui-polish-design.md`（已由用户确认）

**验证命令:** `./gradlew :shared:desktopTest --rerun-tasks :androidApp:assembleDebug`（预期 BUILD SUCCESSFUL，52 测试：50 旧 + Task1 repo 1 个 + Task2 VM 1 个）

---

### Task 1: flag 数据层（schema + 模型 + 仓库）

**Files:**
- Modify: `shared/src/commonMain/sqldelight/com/myapplication/shared/data/TodoDb.sq`
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/domain/model/TodoItem.kt`
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/domain/repository/TodoRepository.kt`
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/data/TodoRepositoryImpl.kt`
- Test: `shared/src/desktopTest/kotlin/com/myapplication/shared/data/TodoRepositoryImplTest.kt`

- [ ] **Step 1: 加 schema 列和 SQL**

在 `TodoDb.sq` 的 `todo` 表定义中 `sort_position` 行后加：

```sql
  flag INTEGER NOT NULL DEFAULT 0,
```

文件末尾追加：

```sql
updateFlag:
UPDATE todo SET flag = ? WHERE id = ?;
```

（`SELECT *` 查询自动包含新列；`insertTodo` 用 DEFAULT 0，无需改。）

- [ ] **Step 2: 模型加字段**

`TodoItem.kt` 的 `isCompleted` 后加 `val flag: Boolean,`：

```kotlin
data class TodoItem(
    val id: Long,
    val listId: Long,
    val title: String,
    val note: String,
    val dueDate: Instant?,
    val isCompleted: Boolean,
    val flag: Boolean,
    val completedAt: Instant?,
    val isTrashed: Boolean,
    val trashedAt: Instant?,
    val parentId: Long?,
    val sortPosition: Double,
    val createdAt: Instant,
)
```

- [ ] **Step 3: 仓库接口加 setFlag**

`TodoRepository.kt` 的 `setCompleted` 后加：

```kotlin
    suspend fun setFlag(id: Long, flag: Boolean)
```

- [ ] **Step 4: 实现 setFlag + 映射**

`TodoRepositoryImpl.kt` 两个 toDomain（`Todo.toDomain` 和 `SelectWithDueDate.toDomain`）在 `isCompleted` 后加 `flag = flag,`。`setCompleted` 后加：

```kotlin
    override suspend fun setFlag(id: Long, flag: Boolean) {
        db.todoDbQueries.updateFlag(flag, id)
    }
```

- [ ] **Step 5: 写失败测试**

`TodoRepositoryImplTest.kt` 末尾（`addListAppendsWithPosition` 后）加：

```kotlin
    @Test
    fun setFlagPersistsAndDefaults() = runTest {
        val repo = newRepo()
        val inbox = repo.inbox()
        repo.addTodo(inbox, "带旗标", "", null, null)
        val item = repo.observeAllActive().first().first()
        assertFalse(item.flag)
        repo.setFlag(item.id, true)
        assertTrue(repo.observeTodo(item.id).first()?.flag == true)
        repo.setFlag(item.id, false)
        assertFalse(repo.observeTodo(item.id).first()?.flag ?: true)
    }
```

（`assertFalse`/`assertTrue` 已 import。）

- [ ] **Step 6: 跑测试确认失败**

Run: `./gradlew :shared:desktopTest --tests "com.myapplication.shared.data.TodoRepositoryImplTest.setFlagPersistsAndDefaults" --rerun-tasks`
Expected: FAIL（`flag` 未生成——SQLDelight 生成代码缺列或模型字段）

- [ ] **Step 7: 跑测试确认通过**

Run: `./gradlew :shared:desktopTest --tests "com.myapplication.shared.data.TodoRepositoryImplTest" --rerun-tasks`
Expected: PASS（14 旧测试 + 1 新测试）

- [ ] **Step 8: 提交**

```bash
git add -A
git commit -m "feat(data): add flag column and setFlag"
```

---

### Task 2: ViewModel 层 flag 切换

**Files:**
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/ui/main/MainViewModel.kt`
- Test: `shared/src/commonTest/kotlin/com/myapplication/shared/ui/main/MainViewModelTest.kt`

- [ ] **Step 1: 写失败测试**

`MainViewModelTest.kt` 的 `FakeRepository` 加字段和方法：

```kotlin
    var flaggedId: Long? = null
    var flaggedValue: Boolean? = null
```
`setCompleted` 后加：
```kotlin
    override suspend fun setFlag(id: Long, flag: Boolean) {
        flaggedId = id
        flaggedValue = flag
    }
```

类内 `toggleCompletedDelegates` 后加：

```kotlin
    @Test
    fun toggleFlagDelegates() = runTest(dispatcher) {
        val repo = FakeRepository()
        val vm = MainViewModel(repo)
        collect(vm)
        val item = TodoItem(6, 1, "x", "", null, false, false, null, false, null, null, 0.0, Instant.fromEpochMilliseconds(0))
        vm.toggleFlag(item)
        advanceUntilIdle()
        assertEquals(6L, repo.flaggedId)
        assertEquals(true, repo.flaggedValue)
    }
```

（注意 TodoItem 构造参数多了 `flag`：位置在 `isCompleted` 后。）

- [ ] **Step 2: 跑测试确认失败**

Run: `./gradlew :shared:desktopTest --tests "com.myapplication.shared.ui.main.MainViewModelTest.toggleFlagDelegates" --rerun-tasks`
Expected: FAIL（`toggleFlag` 未定义）

- [ ] **Step 3: 实现 toggleFlag**

`MainViewModel.kt` 的 `toggleCompleted` 后加：

```kotlin
    fun toggleFlag(item: TodoItem) {
        viewModelScope.launch { repository.setFlag(item.id, !item.flag) }
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `./gradlew :shared:desktopTest --tests "com.myapplication.shared.ui.main.MainViewModelTest" --rerun-tasks`
Expected: PASS（9 测试）

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat(ui): add toggleFlag to MainViewModel"
```

---

### Task 3: DesignTokens 扩展 + RemIcons 新图标

**Files:**
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/ui/theme/DesignTokens.kt`
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/ui/components/RemIcons.kt`
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/ui/components/RemBadge.kt`

- [ ] **Step 1: 新增 token**

`DesignTokens.kt` 的 `RemColors` 加字段：

```kotlin
    val todayBadgeBg: Color,
    val todayBadgeText: Color,
    val upcomingBadgeBg: Color,
    val upcomingBadgeText: Color,
    val overdueBadgeBg: Color,
    val overdueBadgeText: Color,
    val cardBg: Color,
    val cardBorder: Color,
    val hoverActionBg: Color,
    val flagColor: Color,
    val windowBg: Color,
```

亮色：
```kotlin
    todayBadgeBg = Color(0x33FFD60A),
    todayBadgeText = Color(0xFFB25000),
    upcomingBadgeBg = Color(0x20C7C7CC),
    upcomingBadgeText = Color(0xFF8E8E93),
    overdueBadgeBg = Color(0x26FF3B30),
    overdueBadgeText = Color(0xFFFF3B30),
    cardBg = Color(0xFFFFFFFF),
    cardBorder = Color(0xFFEEEEF0),
    hoverActionBg = Color(0xFFE5E5EA),
    flagColor = Color(0xFFFF9500),
    windowBg = Color(0xFFF6F6F8),
```

暗色：
```kotlin
    todayBadgeBg = Color(0x40FFD60A),
    todayBadgeText = Color(0xFFFFD60A),
    upcomingBadgeBg = Color(0x333A3A3C),
    upcomingBadgeText = Color(0xFF8E8E93),
    overdueBadgeBg = Color(0x33FF453A),
    overdueBadgeText = Color(0xFFFF453A),
    cardBg = Color(0xFF2C2C2E),
    cardBorder = Color(0xFF3A3A3C),
    hoverActionBg = Color(0xFF48484A),
    flagColor = Color(0xFFFFD60A),
    windowBg = Color(0xFF1C1C1E),
```

`RemType` 加：
```kotlin
    val title28 = TextStyle(fontFamily = FontFamily.Default, fontSize = 28.sp, fontWeight = FontWeight.Bold, letterSpacing = (-0.5).sp)
```

- [ ] **Step 2: 新增图标枚举 + 绘制**

`RemIcons.kt` 枚举加 `Flag`：

```kotlin
enum class IconName { Calendar, Today, Scheduled, Tray, CheckCircle, Trash, Search, Plus, Close, ChevronBack, ChevronRight, ChevronDown, Flag }
```

`when` 分支加（旗形：三角旗 + 旗杆，填充）：

```kotlin
            IconName.Flag -> {
                line(8f, 4f, 8f, 20f)
                poly(8f, 5.5f, 18f, 8.5f, 8f, 11.5f)
            }
```

- [ ] **Step 3: RemBadge 支持可点击（详情页清除时间用）**

读 `RemBadge.kt` 当前签名，在其上加可选参数：

```kotlin
@Composable
fun RemBadge(
    label: String,
    modifier: Modifier = Modifier,
    tint: Color? = null,
    bg: Color? = null,
    icon: @Composable (() -> Unit)? = null,
    onClick: (() -> Unit)? = null,
) {
```

实现：外层用 `clickable`（onClick != null 时，interactionSource + indication=null），bg 参数优先于默认（默认 = `todayBadgeBg`？不——保持现有默认逻辑，只有传 bg 才覆盖）。查看现有实现后最小改动：加 `bg` 与 `onClick`。

- [ ] **Step 4: 跑测试 + 提交**

Run: `./gradlew :shared:desktopTest --rerun-tasks :androidApp:assembleDebug`
Expected: BUILD SUCCESSFUL（现有测试全绿——本轮无行为变化）

```bash
git add -A
git commit -m "feat(ui): add flag icon, badge tokens and clickable badge"
```

---

### Task 4: TodoRow 双行式重构（行内元数据）

**Files:**
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/ui/todolist/TodoListScreen.kt`

- [ ] **Step 1: 新增辅助函数（文件底部 TodoRow 前）**

```kotlin
@Composable
private fun TodoBadge(item: TodoItem, today: kotlinx.datetime.LocalDate) {
    val colors = LocalRemColors.current
    val due = item.dueDate ?: return
    val date = due.toLocalDateTime(TimeZone.currentSystemDefault()).date
    val bucket = bucketOf(date, today)
    val label = formatDueDate(due)
    val (bg, fg) = when (bucket) {
        DueBucket.OVERDUE -> colors.overdueBadgeBg to colors.overdueBadgeText
        DueBucket.TODAY -> colors.todayBadgeBg to colors.todayBadgeText
        else -> colors.upcomingBadgeBg to colors.upcomingBadgeText
    }
    RemBadge(
        label = label,
        bg = bg,
        tint = fg,
        icon = { RemIcon(IconName.Calendar, fg, Modifier.size(10.dp)) },
    )
}
```

- [ ] **Step 2: 重写 TodoRow 主体**

把 `TodoRow` 的 `Column` 部分改为双行：

```kotlin
        RemCheckbox(item.isCompleted, { mainVm.toggleCompleted(item) })
        Spacer(Modifier.width(10.dp))
        Column(Modifier.weight(1f).clickable { mainVm.openDetail(item.id) }) {
            Row(verticalAlignment = Alignment.CenterVertically) {
                androidx.compose.foundation.text.BasicText(
                    item.title,
                    style = RemType.text13.copy(
                        color = if (item.isCompleted) colors.textTertiary else colors.textPrimary,
                        textDecoration = if (item.isCompleted) TextDecoration.LineThrough else null,
                        fontWeight = if (item.isCompleted) FontWeight.Normal else FontWeight.Medium,
                    ),
                    modifier = Modifier.weight(1f, fill = false),
                )
                if (item.flag) {
                    RemIcon(IconName.Flag, colors.flagColor, Modifier.size(14.dp))
                    Spacer(Modifier.width(6.dp))
                }
            }
            if (item.isCompleted) {
                item.completedAt?.let {
                    androidx.compose.foundation.text.BasicText(
                        "已完成 ${formatDueDate(it, TimeZone.currentSystemDefault(), today)}",
                        style = RemType.text12.copy(color = colors.textTertiary),
                    )
                }
            } else {
                val subCount = remember(item.id) { 0 }
                if (item.note.isNotBlank()) {
                    androidx.compose.foundation.text.BasicText(
                        item.note,
                        style = RemType.text12.copy(color = colors.textTertiary),
                        maxLines = 1,
                        overflow = TextOverflow.Ellipsis,
                        modifier = Modifier.weight(1f),
                    )
                }
            }
        }
        if (showChevron) {
            RemIcon(if (expanded) IconName.ChevronDown else IconName.ChevronRight, colors.textTertiary, Modifier.size(14.dp))
            Spacer(Modifier.width(8.dp))
        }
        TodoBadge(item, today)
```

说明：行内子任务计数需父行知道子任务数——`TodoListScreen` 的 `PlainList`/`ScheduledGrouped` 已有 `childrenByParent`。给 `TodoRow` 加参数 `subtaskCount: Int = 0`，在第二行右侧显示 `"⌄ $subtaskCount"`。`PlainList` 传 `childrenByParent[parent.id]?.size ?: 0`；`ScheduledGrouped` 无法分组子任务（子任务无 dueDate 不进 scheduled），默认 0；`TrashList` 默认 0。同时在 `TodoListScreen.kt` 增加 import：`androidx.compose.ui.text.font.FontWeight`、`androidx.compose.ui.text.style.TextOverflow`。

第二行最终结构（未完成时）：

```kotlin
            if (item.note.isNotBlank() || subtaskCount > 0) {
                Row(verticalAlignment = Alignment.CenterVertically) {
                    if (item.note.isNotBlank()) {
                        androidx.compose.foundation.text.BasicText(
                            item.note,
                            style = RemType.text12.copy(color = colors.textTertiary),
                            maxLines = 1,
                            overflow = TextOverflow.Ellipsis,
                            modifier = Modifier.weight(1f),
                        )
                    } else {
                        Spacer(Modifier.weight(1f))
                    }
                    if (subtaskCount > 0) {
                        Spacer(Modifier.width(6.dp))
                        androidx.compose.foundation.text.BasicText(
                            "⌄ $subtaskCount",
                            style = RemType.text12.copy(color = colors.textTertiary),
                        )
                    }
                }
            }
```

- [ ] **Step 3: 调用点传 subtaskCount**

`PlainList` 两处 TodoRow 调用传 `subtaskCount = childrenByParent[parent.id]?.size ?: 0`（父行；子行不需要传——子任务自身无子任务，保持默认）。`ScheduledGrouped`/`TrashList` 不改（默认 0）。

- [ ] **Step 4: 验证 + 冒烟**

Run: `./gradlew :shared:desktopTest --rerun-tasks :androidApp:assembleDebug`
Expected: BUILD SUCCESSFUL，50 测试（无新测试——UI 层）

手动冒烟：`./gradlew :desktopApp:run`——检查行内徽章/旗标/备注预览，然后退出。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat(ui): two-line todo rows with metadata badges"
```

---

### Task 5: 列表页头部 + Sticky 分区白卡片

**Files:**
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/ui/todolist/TodoListScreen.kt`
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/util/Formatting.kt`（如需）

- [ ] **Step 1: Formatting 增加"今天视图"分区所需的 bucket 标签函数**

`bucketLabel` 已覆盖 OVERDUE/TODAY/TOMORROW/THIS_WEEK/LATER（"过期/今天/明天/本周/计划"）。spec 的今天视图分区是 过期/今天/以后 三组——用现有 bucketLabel 直接显示即可，不新增函数。**跳过本步。**

- [ ] **Step 2: 重写列表头（TodoListScreen 的 Column 顶部）**

替换 `BasicText(scopeTitle...)` + 计数副行 为：

```kotlin
    val colors = LocalRemColors.current
    Column(modifier.background(colors.windowBg)) {
        Row(
            Modifier
                .fillMaxWidth()
                .padding(start = 20.dp, end = 16.dp, top = 18.dp),
            verticalAlignment = Alignment.CenterVertically,
        ) {
            androidx.compose.foundation.text.BasicText(
                scopeTitle(scope, query),
                style = RemType.title28.copy(color = colors.textPrimary),
                modifier = Modifier.weight(1f),
            )
            val count = if (scope == Scope.Completed) todos.size else activeCount
            if (count > 0) {
                RemBadge(
                    label = "$count 项",
                    bg = if (scope == Scope.Today) colors.overdueBadgeBg else colors.upcomingBadgeBg,
                    tint = if (scope == Scope.Today) colors.overdueBadgeText else colors.upcomingBadgeText,
                )
            }
            Spacer(Modifier.width(8.dp))
            Box(
                Modifier
                    .size(26.dp)
                    .clip(RoundedCornerShape(RemRadii.r6))
                    .background(colors.hoverActionBg)
                    .clickable(interactionSource = remember { MutableInteractionSource() }, indication = null) { /* 聚焦快速输入：见 Step 3 */ }
                    .semantics { contentDescription = "新建待办" },
                contentAlignment = Alignment.Center,
            ) {
                RemIcon(IconName.Plus, colors.textPrimary, Modifier.size(14.dp))
            }
        }
        androidx.compose.foundation.text.BasicText(
            if (scope == Scope.Today || scope == Scope.Scheduled) "${today} · 星期${"一二三四五六日"[today.dayOfWeek.isoDayNumber - 1]}" else "$activeCount 项未完成",
            style = RemType.text13.copy(color = colors.textTertiary),
            modifier = Modifier.padding(start = 20.dp, top = 4.dp, bottom = 12.dp),
        )
```

import 增补：`androidx.compose.foundation.shape.RoundedCornerShape`、`androidx.compose.ui.semantics.contentDescription`、`androidx.compose.ui.semantics.semantics`、`com.myapplication.shared.ui.theme.RemRadii`。

- [ ] **Step 3: QuickAddRow 支持外部聚焦**

`QuickAddRow` 加参数：

```kotlin
@Composable
private fun QuickAddRow(mainVm: MainViewModel, focusRequested: Boolean = false) {
```

内部（RemTextField 前）：
```kotlin
    val focusRequester = remember { FocusRequester() }
    LaunchedEffect(focusRequested) { if (focusRequested) focusRequester.requestFocus() }
```
RemTextField 加 `focusRequester = focusRequester`（先确认 RemTextField 是否支持；不支持则在本步给 RemTextField 加该参数，透传给 BasicTextField 的 Modifier.focusRequester）。

`TodoListScreen` 加 `var quickAddFocus by remember { mutableStateOf(0) }`，头部 ＋按钮 onClick 改为 `quickAddFocus++`，QuickAddRow 传 `focusRequested = quickAddFocus > 0`。注意：＋按钮 onClick 目前空实现——替换之。

- [ ] **Step 4: 分区白卡片（今天/已完成视图）**

把 `TodoListScreen` 的 `when` 分支改为调用新函数：

```kotlin
        when {
            todos.isEmpty() && query.isNotBlank() -> RemEmptyState("没有找到结果", "换个关键词试试", IconName.Search)
            todos.isEmpty() -> RemEmptyState("没有待办", "点击 ＋ 或输入框添加", IconName.Tray)
            scope == Scope.Today -> TodayGrouped(todos, today, mainVm)
            scope == Scope.Scheduled -> ScheduledGrouped(todos, today, mainVm)
            scope == Scope.Trash -> TrashList(todos, mainVm)
            else -> PlainList(todos, today, mainVm)
        }
```

新增 `TodayGrouped`（仿 ScheduledGrouped，但分区固定为 OVERDUE/TODAY/LATER 三组，白卡片包裹）：

```kotlin
@Composable
private fun TodayGrouped(todos: List<TodoItem>, today: kotlinx.datetime.LocalDate, mainVm: MainViewModel) {
    val tz = TimeZone.currentSystemDefault()
    val grouped = todos
        .filter { it.dueDate != null }
        .groupBy { bucketOf(it.dueDate!!.toLocalDateTime(tz).date, today) }
    LazyColumn(Modifier.fillMaxWidth().padding(horizontal = 16.dp)) {
        listOf(DueBucket.OVERDUE, DueBucket.TODAY, DueBucket.LATER).forEach { bucket ->
            val items = grouped[bucket].orEmpty()
            if (items.isNotEmpty()) {
                item(key = "h-$bucket") { SectionHeader(bucketLabel(bucket), items.size, bucket == DueBucket.OVERDUE) }
                item(key = "c-$bucket") {
                    Column(
                        Modifier
                            .fillMaxWidth()
                            .background(colors.cardBg, RoundedCornerShape(RemRadii.r10))
                            .border(1.dp, colors.cardBorder, RoundedCornerShape(RemRadii.r10))
                            .padding(horizontal = 8.dp),
                    ) {
                        items.forEach { item ->
                            TodoRow(item, mainVm, today, showChevron = false)
                        }
                    }
                }
                Spacer(Modifier.height(12.dp))
            }
        }
    }
}
```

（需 `import androidx.compose.foundation.border`、`RoundedCornerShape`、`DueBucket`、`bucketLabel`。）

新增 `SectionHeader`：

```kotlin
@Composable
private fun SectionHeader(title: String, count: Int, overdue: Boolean) {
    val colors = LocalRemColors.current
    Row(Modifier.fillMaxWidth().padding(vertical = 10.dp), verticalAlignment = Alignment.CenterVertically) {
        androidx.compose.foundation.text.BasicText(
            title,
            style = RemType.label13.copy(color = colors.textPrimary),
        )
        if (count > 0) {
            Spacer(Modifier.width(6.dp))
            RemBadge(
                label = "$count",
                bg = if (overdue) colors.overdueBadgeBg else colors.upcomingBadgeBg,
                tint = if (overdue) colors.overdueBadgeText else colors.upcomingBadgeText,
            )
        }
        Spacer(Modifier.width(8.dp))
        Box(Modifier.weight(1f).height(1.dp).background(colors.rowDivider))
    }
}
```

- [ ] **Step 5: PlainList 也改白卡片（待办/已完成两组）**

重写 `PlainList`：

```kotlin
@Composable
private fun PlainList(todos: List<TodoItem>, today: kotlinx.datetime.LocalDate, mainVm: MainViewModel) {
    val colors = LocalRemColors.current
    val active = todos.filter { !it.isCompleted && it.parentId == null }
    val childrenByParent = todos.filter { it.parentId != null }.groupBy { it.parentId!! }
    val completed = todos.filter { it.isCompleted && it.parentId == null }
    var expanded by remember { mutableStateOf(true) }

    LazyColumn(Modifier.fillMaxWidth().padding(horizontal = 16.dp)) {
        if (active.isNotEmpty()) {
            item(key = "active-card") {
                Column(
                    Modifier
                        .fillMaxWidth()
                        .background(colors.cardBg, RoundedCornerShape(RemRadii.r10))
                        .border(1.dp, colors.cardBorder, RoundedCornerShape(RemRadii.r10))
                        .padding(horizontal = 8.dp),
                ) {
                    active.forEach { parent ->
                        TodoRow(parent, mainVm, today, showChevron = childrenByParent[parent.id] != null, expanded = expanded, onToggleExpand = { expanded = !expanded }, subtaskCount = childrenByParent[parent.id]?.size ?: 0)
                        if (expanded) {
                            childrenByParent[parent.id]?.forEach { child ->
                                TodoRow(child, mainVm, today, indent = true)
                            }
                        }
                    }
                }
            }
            Spacer(Modifier.height(12.dp))
        }
        if (completed.isNotEmpty()) {
            item(key = "completed-header") {
                Row(Modifier.fillMaxWidth().padding(vertical = 6.dp), verticalAlignment = Alignment.CenterVertically) {
                    androidx.compose.foundation.text.BasicText("已完成", style = RemType.label13.copy(color = colors.textTertiary))
                    Spacer(Modifier.weight(1f))
                    RemIcon(if (expanded) IconName.ChevronDown else IconName.ChevronRight, colors.textTertiary, Modifier.size(14.dp))
                }
            }
            if (expanded) {
                item(key = "completed-card") {
                    Column(
                        Modifier
                            .fillMaxWidth()
                            .background(colors.cardBg, RoundedCornerShape(RemRadii.r10))
                            .border(1.dp, colors.cardBorder, RoundedCornerShape(RemRadii.r10))
                            .padding(horizontal = 8.dp),
                    ) {
                        completed.forEach { item -> TodoRow(item, mainVm, today) }
                    }
                }
            }
        }
    }
}
```

`ScheduledGrouped` 同步改造：分区用 `bucketLabel` + SectionHeader + 白卡片（分区 THIS_WEEK 合并进"计划"？不——spec 只说 smart 视图分区，ScheduledGrouped 保留现有 5 桶，但视觉改为白卡片包裹，SectionHeader 复用）。保持 5 桶。

- [ ] **Step 6: 验证 + 冒烟**

Run: `./gradlew :shared:desktopTest --rerun-tasks :androidApp:assembleDebug`
Expected: BUILD SUCCESSFUL

冒烟：`./gradlew :desktopApp:run`，检查今天视图（有过期/今天数据时）分区卡片 + 头部大标题 + ＋按钮聚焦。

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "feat(ui): list header with big title and section cards"
```

---

### Task 6: 侧边栏增强

**Files:**
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/ui/sidebar/Sidebar.kt`

- [ ] **Step 1: 分组折叠 + 我的列表头部**

`Sidebar` 加状态 `var listsExpanded by remember { mutableStateOf(true) }`。把 `"我的列表"` 标签行替换为可点击头部：

```kotlin
        Row(
            Modifier
                .fillMaxWidth()
                .padding(horizontal = 8.dp, vertical = 4.dp)
                .clickable { listsExpanded = !listsExpanded },
            verticalAlignment = Alignment.CenterVertically,
        ) {
            RemIcon(IconName.ChevronRight, colors.textTertiary, Modifier.size(12.dp).graphicsLayer { rotationZ = if (listsExpanded) 90f else 0f })
            Spacer(Modifier.width(4.dp))
            androidx.compose.foundation.text.BasicText("我的列表", style = RemType.label12.copy(color = colors.textTertiary))
        }
        if (listsExpanded) {
            lists.forEach { list -> ... }
        }
```

（`import androidx.compose.ui.graphics.graphicsLayer`）

- [ ] **Step 2: 今天红色计数 + 各列表计数**

`ScopeRow` 已显示 count。给"今天"行传红底白字胶囊（spec §5）。`ScopeRow` 加参数 `countBadge: Boolean = false`，`count > 0` 时：

```kotlin
        if (count > 0) {
            if (countBadge) {
                Box(
                    Modifier
                        .background(colors.danger, RoundedCornerShape(RemRadii.r8))
                        .padding(horizontal = 6.dp, vertical = 1.dp),
                ) {
                    androidx.compose.foundation.text.BasicText(count.toString(), style = RemType.text12.copy(color = Color.White))
                }
            } else {
                androidx.compose.foundation.text.BasicText(count.toString(), style = RemType.text12.copy(color = if (selected) colors.accent else colors.textTertiary))
            }
        }
```

调用处"今天"行加 `countBadge = true`。

- [ ] **Step 3: ListRow hover 操作按钮**

`ListRow` 的 Row 内 count 后加 hover 按钮组：

```kotlin
        if (hovered) {
            Spacer(Modifier.width(4.dp))
            Box(
                Modifier
                    .size(16.dp)
                    .clip(RoundedCornerShape(RemRadii.r4))
                    .clickable { onSelect() /* 快捷跳转；新建聚焦由主输入承担 */ },
                contentAlignment = Alignment.Center,
            ) {
                RemIcon(IconName.Plus, colors.textTertiary, Modifier.size(12.dp))
            }
            if (canDelete) {
                Spacer(Modifier.width(2.dp))
                Box(
                    Modifier
                        .size(16.dp)
                        .clip(RoundedCornerShape(RemRadii.r4))
                        .background(if (hovered) colors.hoverActionBg else Color.Transparent)
                        .clickable { confirmDelete = true },
                    contentAlignment = Alignment.Center,
                ) {
                    RemIcon(IconName.Trash, colors.textTertiary, Modifier.size(12.dp))
                }
            }
        }
```

注意 clickable 无 indication——用 `interactionSource = remember { MutableInteractionSource() }, indication = null` 保持一致。＋按钮语义：spec 说"新建（跳转列表并聚焦 quick-add）"——简化为 `onSelect()` + 主界面输入框已常驻，即可满足"跳转列表"。删除按钮走 `confirmDelete` 确认对话框（复用现有）。

- [ ] **Step 4: 验证 + 冒烟**

Run: `./gradlew :shared:desktopTest --rerun-tasks :androidApp:assembleDebug`
Expected: BUILD SUCCESSFUL

冒烟：hover 列表行出现 ＋/垃圾桶；折叠"我的列表"。

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat(ui): sidebar groups, badges and hover actions"
```

---

### Task 7: 详情页结合版

**Files:**
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/ui/detail/DetailScreen.kt`
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/ui/components/RemDatePicker.kt`
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/ui/detail/DetailViewModel.kt`
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/domain/repository/TodoRepository.kt`（如 setDueDate 语义需变）
- Test: `shared/src/desktopTest/kotlin/com/myapplication/shared/data/TodoRepositoryImplTest.kt`

- [ ] **Step 1: DetailViewModel 加 setTime**

读 `DetailViewModel.kt` 后，加方法（仿 setDueDate，组合 date+time 再写）：

```kotlin
    fun setTime(hour: Int, minute: Int) {
        val current = todo.value ?: return
        val base = current.dueDate
            ?.toLocalDateTime(TimeZone.currentSystemDefault())
            ?: LocalDateTime(todayDate(), LocalTime(hour, minute))
        val ldt = LocalDateTime(base.date, LocalTime(hour, minute))
        viewModelScope.launch { repository.setDueDate(ldt.toInstant(TimeZone.currentSystemDefault())) }
    }

    fun setTimeNull() {
        val current = todo.value ?: return
        if (current.dueDate == null) return
        val ldt = current.dueDate.toLocalDateTime(TimeZone.currentSystemDefault())
        val noTime = LocalDateTime(ldt.date, LocalTime(0, 0))
        viewModelScope.launch { repository.setDueDate(noTime.toInstant(TimeZone.currentSystemDefault())) }
    }
```

- [ ] **Step 2: RemDatePicker 加时间行**

在 `RemDatePicker` 的日历网格后、`RemDialog` content 内加：

```kotlin
            Spacer(Modifier.height(12.dp))
            TimePickerRow(initialTime = initialDate?.let { null } ?: null, onPickTime = { h, m -> onPickTime(h, m) })
```

签名变更：`RemDatePicker(initialDate, initialTime: LocalTime?, onPick: (LocalDate) -> Unit, onPickTime: (Int, Int) -> Unit, onDismiss)`。为兼容旧调用，加默认参数 `initialTime: LocalTime? = null, onPickTime: (Int, Int) -> Unit = { _, _ -> }`。

新增 `TimePickerRow`：

```kotlin
@Composable
private fun TimePickerRow(initialTime: LocalTime?, onPickTime: (Int, Int) -> Unit) {
    val colors = LocalRemColors.current
    var hour by remember { mutableStateOf(initialTime?.hour ?: 9) }
    var minute by remember { mutableStateOf(initialTime?.minute ?: 0) }
    Row(Modifier.fillMaxWidth().padding(vertical = 4.dp), verticalAlignment = Alignment.CenterVertically) {
        androidx.compose.foundation.text.BasicText("时间", style = RemType.text13.copy(color = colors.textSecondary))
        Spacer(Modifier.weight(1f))
        RemIconButton(IconName.ChevronBack, "减一小时", onClick = { hour = (hour + 23) % 24 }, size = 14.dp)
        androidx.compose.foundation.text.BasicText(
            "${hour.toString().padStart(2, '0')} : ${minute.toString().padStart(2, '0')}",
            style = RemType.title15.copy(color = colors.textPrimary),
        )
        RemIconButton(IconName.ChevronRight, "加一小时", onClick = { hour = (hour + 1) % 24 }, size = 14.dp)
        Spacer(Modifier.width(4.dp))
        RemIconButton(IconName.ChevronBack, "减五分钟", onClick = { minute = (minute + 55) % 60 }, size = 14.dp)
        androidx.compose.foundation.text.BasicText("分", style = RemType.text12.copy(color = colors.textTertiary))
        RemIconButton(IconName.ChevronRight, "加五分钟", onClick = { minute = (minute + 5) % 60 }, size = 14.dp)
        Spacer(Modifier.width(8.dp))
        RemButton("清除时间", onClick = { onPickTime(-1, -1) })
    }
}
```

说明：`onPickTime(-1, -1)` = 清除时间（00:00 语义）；正常值 0..23 / 0..59。

- [ ] **Step 3: DetailScreen 标题行 + 时间徽章 + 备注块**

标题 Row 内（RemTextField 后、关闭按钮前）加时间徽章：

```kotlin
            if (current.dueDate != null) {
                Spacer(Modifier.width(8.dp))
                RemBadge(
                    label = formatDueDate(current.dueDate),
                    bg = colors.upcomingBadgeBg,
                    tint = colors.upcomingBadgeText,
                    onClick = { showDatePicker = true },
                )
            }
```

备注 RemTextField 外包一层质感块：

```kotlin
        Box(
            Modifier
                .fillMaxWidth()
                .background(colors.cardBg, RoundedCornerShape(RemRadii.r6))
                .border(1.dp, colors.cardBorder, RoundedCornerShape(RemRadii.r6))
                .padding(4.dp),
        ) {
            RemTextField(
                value = noteText,
                onValueChange = { noteText = it; detailVm.setNote(it) },
                placeholder = "备注…",
                singleLine = false,
                minLines = 3,
                filled = false,
                modifier = Modifier.fillMaxWidth(),
            )
        }
```

（import `androidx.compose.foundation.border`、`RoundedCornerShape`。）

- [ ] **Step 4: 旗标行 + 日期/列表/子任务行图标 + 底部操作区**

日期行与列表行之间插入旗标行：

```kotlin
        Row(
            Modifier
                .fillMaxWidth()
                .clickable(
                    interactionSource = remember { MutableInteractionSource() },
                    indication = null,
                ) { mainVm.toggleFlag(current) }
                .drawBehind { drawLine(colors.rowDivider, Offset(0f, size.height), Offset(size.width, size.height), 1f) }
                .padding(vertical = 10.dp),
            verticalAlignment = Alignment.CenterVertically,
        ) {
            androidx.compose.foundation.text.BasicText("旗标", style = RemType.text13.copy(color = colors.textSecondary))
            Spacer(Modifier.weight(1f))
            RemIcon(IconName.Flag, if (current.flag) colors.flagColor else colors.textTertiary, Modifier.size(16.dp))
            Spacer(Modifier.width(4.dp))
            androidx.compose.foundation.text.BasicText(if (current.flag) "已标记" else "未标记", style = RemType.text13.copy(color = if (current.flag) colors.textPrimary else colors.textTertiary))
        }
```

日期/列表/子任务标签加图标前缀（在 BasicText 前加 RemIcon，12dp 间距）：

```kotlin
            RemIcon(IconName.Calendar, colors.textTertiary, Modifier.size(14.dp))
            Spacer(Modifier.width(8.dp))
            androidx.compose.foundation.text.BasicText("日期", ...)
```
（列表行用 `IconName.Tray`、子任务行用 `IconName.ChevronDown`；"子任务"标签区保留现有"子任务 ⌄ 3"折叠——子任务计数行加 `RemIcon(IconName.ChevronDown, ...)` 前缀。）

底部（"移到垃圾箱"按钮上方）加操作区：

```kotlin
        Row(Modifier.fillMaxWidth().padding(top = 8.dp)) {
            RemButton("移到列表", onClick = { showListDialog = true }, modifier = Modifier.weight(1f))
            Spacer(Modifier.width(8.dp))
            RemButton(
                "移到垃圾箱",
                onClick = {
                    mainVm.trash(current)
                    mainVm.back()
                },
                danger = true,
                modifier = Modifier.weight(1f),
            )
        }
```

删除原单个"移到垃圾箱"按钮。

- [ ] **Step 5: 完成时间 + 创建时间展示**

标题行下方（RemCheckbox Row 之后）加：

```kotlin
        if (current.isCompleted && current.completedAt != null) {
            Spacer(Modifier.height(4.dp))
            androidx.compose.foundation.text.BasicText(
                "已完成 ${formatDueDate(current.completedAt)}",
                style = RemType.text12.copy(color = colors.textTertiary),
            )
        }
```

详情页底部（操作区前）加创建时间：

```kotlin
        Spacer(Modifier.height(8.dp))
        androidx.compose.foundation.text.BasicText(
            "创建于 ${current.createdAt.toLocalDateTime(TimeZone.currentSystemDefault()).let { "${it.monthNumber} 月 ${it.dayOfMonth} 日" }}",
            style = RemType.text12.copy(color = colors.textTertiary),
        )
```

- [ ] **Step 6: 日期弹窗接线**

`showDatePicker` 的 RemDatePicker 调用改为：

```kotlin
    if (showDatePicker) {
        RemDatePicker(
            initialDate = current?.dueDate?.toLocalDateTime(TimeZone.currentSystemDefault())?.date,
            initialTime = current?.dueDate?.toLocalDateTime(TimeZone.currentSystemDefault())?.time?.takeIf { !(it.hour == 0 && it.minute == 0) },
            onPick = { date ->
                val time = current?.dueDate
                    ?.toLocalDateTime(TimeZone.currentSystemDefault())?.time
                    ?: LocalTime(9, 0)
                detailVm.setDueDate(LocalDateTime(date, time))
            },
            onPickTime = { h, m ->
                if (h == -1 && m == -1) detailVm.setTimeNull() else detailVm.setTime(h, m)
            },
            onDismiss = { showDatePicker = false },
        )
    }
```

- [ ] **Step 7: 验证 + 冒烟**

Run: `./gradlew :shared:desktopTest --rerun-tasks :androidApp:assembleDebug`
Expected: BUILD SUCCESSFUL

冒烟：开详情——标题行时间徽章、备注块、旗标行切换、日期弹窗时间步进、底部双按钮。

- [ ] **Step 8: 提交**

```bash
git add -A
git commit -m "feat(ui): rich detail panel with flag, time picker and actions"
```

---

### Task 8: 空状态线稿插画

**Files:**
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/ui/components/RemEmptyState.kt`

- [ ] **Step 1: 重写插画（Canvas 线稿）**

`RemEmptyState` 的 icon 分支替换为线稿插画（斜置圆角卡片 + 勾，方案 B）：

```kotlin
        if (icon != null) {
            Canvas(Modifier.size(96.dp)) {
                val s = size.minDimension
                val st = 2.dp.toPx()
                val cardColor = colors.textTertiary.copy(alpha = 0.35f)
                val g = androidx.compose.ui.graphics.drawscope.withTransform({
                    rotate(-6f, pivot = Offset(s / 2, s / 2))
                }) {
                    drawRoundRect(
                        color = cardColor,
                        topLeft = Offset(s * 0.18f, s * 0.22f),
                        size = Size(s * 0.64f, s * 0.56f),
                        cornerRadius = CornerRadius(s * 0.08f),
                        style = Stroke(width = st),
                    )
                    drawRoundRect(
                        color = colors.checkboxBorder.copy(alpha = 0.5f),
                        topLeft = Offset(s * 0.30f, s * 0.34f),
                        size = Size(s * 0.4f, s * 0.08f),
                        cornerRadius = CornerRadius(s * 0.04f),
                    )
                    drawRoundRect(
                        color = colors.checkboxBorder.copy(alpha = 0.5f),
                        topLeft = Offset(s * 0.30f, s * 0.48f),
                        size = Size(s * 0.28f, s * 0.08f),
                        cornerRadius = CornerRadius(s * 0.04f),
                    )
                    val p = Path().apply {
                        moveTo(s * 0.34f, s * 0.74f)
                        lineTo(s * 0.42f, s * 0.82f)
                        lineTo(s * 0.60f, s * 0.62f)
                    }
                    drawPath(p, colors.accent, style = Stroke(width = st, cap = StrokeCap.Round, join = StrokeJoin.Round))
                }
            }
            Spacer(Modifier.height(16.dp))
        }
```

（import：`androidx.compose.foundation.Canvas`、`androidx.compose.ui.geometry.CornerRadius`、`Offset`、`Size`、`androidx.compose.ui.graphics.Path`、`StrokeCap`、`StrokeJoin`、`androidx.compose.ui.graphics.drawscope.Stroke`、`androidx.compose.ui.graphics.drawscope.rotate`。注意 `rotate` 在 `withTransform` lambda 内使用，需要 `androidx.compose.ui.graphics.drawscope.rotate`。）

替换后：原 `RemIcon(icon, ...)` + Spacer 删掉；文案样式：title 用 `RemType.title20` 已有，subtitle 保持。

- [ ] **Step 2: 验证 + 提交**

Run: `./gradlew :shared:desktopTest --rerun-tasks :androidApp:assembleDebug`
Expected: BUILD SUCCESSFUL

```bash
git add -A
git commit -m "feat(ui): line-art empty state illustration"
```

---

### Task 9: 动效与氛围

**Files:**
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/ui/todolist/TodoListScreen.kt`
- Modify: `shared/src/commonMain/kotlin/App.kt`
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/ui/theme/DesignTokens.kt`

- [ ] **Step 1: 窗口底色统一**

`LightRemColors.contentBg = Color(0xFFF6F6F8)`（原 FAFAFC）；暗色 contentBg 保持 2C2C2E。TodoListScreen 外层 Column 已用 `colors.windowBg`（Task 5 Step 2 加了 `.background(colors.windowBg)`）。

- [ ] **Step 2: 详情面板过渡**

`App.kt` 的 wide 分支详情加 `AnimatedVisibility`：

```kotlin
            wide -> {
                Row(Modifier.fillMaxSize()) {
                    Sidebar(mainVm)
                    TodoListScreen(mainVm, Modifier.weight(1f).background(colors.contentBg))
                    androidx.compose.animation.AnimatedVisibility(
                        visible = selectedId != null,
                        enter = androidx.compose.animation.fadeIn(tween(150)) + androidx.compose.animation.slideInHorizontally(initialOffsetX = { it / 8 }),
                        exit = androidx.compose.animation.fadeOut(tween(100)),
                        modifier = Modifier.width(340.dp),
                    ) {
                        selectedId?.let { DetailScreen(mainVm, graph, it) }
                    }
                }
            }
```

（import `androidx.compose.animation.AnimatedVisibility`、`fadeIn`、`fadeOut`、`slideInHorizontally`、`androidx.compose.animation.core.tween`。若 AnimatedVisibility 与 BoxWithConstraints 组合出问题（width 340 导致内容挤压），回退为 `if (selectedId != null) { DetailScreen(...) }`。）

- [ ] **Step 3: 勾选渐隐（跳过——行移除依赖列表重组，LazyColumn animateItem 复杂且非必需。规格 §7 保留此项为可选项，标记为 deferred。）**

- [ ] **Step 4: 细滚动条（桌面）**

桌面端 LazyColumn 加 `verticalScroll` 不适用——改为在 `TodoListScreen` 的三个列表（PlainList/ScheduledGrouped/TodayGrouped）的 LazyColumn modifier 后加：

```kotlin
            .let { m ->
                if (isDesktop) m.verticalScrollbar(rememberScrollbarAdapter(scrollState)) else m
            }
```

实现细节：LazyColumn 需要 `rememberLazyListState()`，用 `Modifier.verticalScrollbar`。**简化：跳过本步**（平台条件渲染复杂，规格为"可选氛围项"——deferred）。

- [ ] **Step 5: 验证 + 提交**

Run: `./gradlew :shared:desktopTest --rerun-tasks :androidApp:assembleDebug`
Expected: BUILD SUCCESSFUL

```bash
git add -A
git commit -m "feat(ui): detail panel transition and unified window bg"
```

---

### Task 10: 暗色同精度核对 + 全量验证

**Files:**
- Modify: `shared/src/commonMain/kotlin/com/myapplication/shared/ui/theme/DesignTokens.kt`（如需）
- Verify: 全部

- [ ] **Step 1: 暗色 token 核对**

对照规格 §8：卡片 `#2C2C2E` 底/`#3A3A3C` 边 ✓（Task 3 已设）；分区线 rowDivider 暗色 `#38383A` ✓；空状态线稿用 checkboxBorder（暗色 636366）✓；badge 透明度 25%+原色 ✓（Task 3 已设 0x40/0x33）。无需改动则跳过。

- [ ] **Step 2: 全量验证**

Run: `./gradlew :shared:desktopTest --rerun-tasks :androidApp:assembleDebug`
Expected: BUILD SUCCESSFUL，**52 测试全绿**（50 旧 + Task1 的 1 个 + Task2 的 1 个 + 其余 UI 任务无新测试）

- [ ] **Step 3: 全量冒烟（亮+暗）**

`./gradlew :desktopApp:run`：亮色验证——三栏、今天分区卡片、行双行式、详情结合版、空状态插画、侧边栏 hover 按钮、折叠分组。然后系统切深色重开 app 验证暗色卡片/徽章。手动切 macOS 外观后重跑。

- [ ] **Step 4: 提交（如有改动）**

```bash
git add -A
git commit -m "style(ui): dark mode parity pass"
```

---

## 自审查记录

- **Spec §3 行内元数据** → Task 4（双行式 + 徽章 + 旗标 + 完成时间 + 子任务计数）
- **Spec §4 头部 + 分区** → Task 5（title28 头部、＋聚焦、Today/Scheduled/Plain 白卡片分区、SectionHeader）
- **Spec §5 侧边栏** → Task 6（折叠分组、红色计数、hover ＋/删除）
- **Spec §6 详情页** → Task 7（时间徽章同行、备注块、旗标行、图标行、底部操作区、完成/创建时间、RemDatePicker 时间行）
- **Spec §7 氛围** → Task 8（空状态线稿）+ Task 9（过渡、窗口底色）；勾选渐隐/细滚动条标记 deferred（可选项）
- **Spec §8 暗色** → Task 3（token）+ Task 10（核对）
- **Spec §9 测试** → Task 1/2（3 个新测试）
- **Spec §2 数据模型** → Task 1（flag 列）

**类型一致性核对**：`TodoItem.flag: Boolean`（Task1 定义，Task2/4/7 使用）；`setFlag(id: Long, flag: Boolean)`（Task1 接口，Task2 实现+测试）；`RemBadge(label, bg, tint, onClick)`（Task3 扩展签名，Task4/5/7 使用）；`RemDatePicker(initialTime, onPickTime)`（Task7 定义，Task7 使用）；`SubtaskCount: Int` 参数（Task4 定义，Task4/5 使用）。
